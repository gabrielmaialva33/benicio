/**
 * Injected by Vite's `define` (see `vite.config.ts`) from `package.json`, so
 * the shell can show which build is live without an extra request.
 */
// eslint-disable-next-line @typescript-eslint/naming-convention -- Vite's define contract uses SCREAMING_SNAKE with sentinel underscores.
declare const __APP_VERSION__: string
